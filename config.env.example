# Configuration de l'application Gare de Train
# Copiez ce fichier vers .env et modifiez les valeurs selon votre environnement

# ===========================================
# CONFIGURATION GÉNÉRALE
# ===========================================

# Clé secrète pour les sessions et CSRF
# Générez une clé sécurisée pour la production
SECRET_KEY=dev-secret-key-change-in-production-2024

# Environnement de l'application
# Valeurs possibles: development, production, testing
FLASK_ENV=development

# Mode debug (True pour le développement, False pour la production)
FLASK_DEBUG=True

# ===========================================
# CONFIGURATION DE LA BASE DE DONNÉES
# ===========================================

# Base de données PostgreSQL (production)
# Décommentez et modifiez la ligne suivante pour utiliser PostgreSQL
#DATABASE_URL=postgresql://username:password@localhost:5432/trainstation

# Base de données SQLite (développement)
# Décommentez la ligne suivante pour utiliser SQLite
#DATABASE_URL=sqlite:///trainstation.db

# ===========================================
# CONFIGURATION POSTGRESQL
# ===========================================

# Informations de connexion PostgreSQL
# Remplacez les valeurs par vos propres informations

# Hôte de la base de données
DB_HOST=localhost

# Port de la base de données
DB_PORT=5432

# Nom de la base de données
DB_NAME=TrainStation

# Nom d'utilisateur PostgreSQL
DB_USERNAME=postgres

# Mot de passe PostgreSQL
DB_PASSWORD=

# ===========================================
# CONFIGURATION DU SERVEUR
# ===========================================

# Adresse IP du serveur (0.0.0.0 pour toutes les interfaces)
HOST=0.0.0.0

# Port du serveur (5001 pour éviter les conflits avec AirPlay)
PORT=5001

# ===========================================
# CONFIGURATION DE SÉCURITÉ
# ===========================================

# Protection CSRF (True par défaut)
WTF_CSRF_ENABLED=True

# ===========================================
# EXEMPLES DE CONFIGURATION
# ===========================================

# Exemple 1: SQLite (développement local)
# DATABASE_URL=sqlite:///trainstation.db

# Exemple 2: PostgreSQL local
# DATABASE_URL=postgresql://postgres:password@localhost:5432/trainstation

# Exemple 3: PostgreSQL avec utilisateur personnalisé
# DATABASE_URL=postgresql://trainuser:trainpass@localhost:5432/trainstation

# Exemple 4: PostgreSQL distant
# DATABASE_URL=postgresql://username:password@your-server.com:5432/trainstation

# ===========================================
# INSTRUCTIONS D'UTILISATION
# ===========================================

# 1. Copiez ce fichier vers .env
# 2. Modifiez les valeurs selon votre environnement
# 3. Ne commitez jamais le fichier .env dans Git
# 4. Pour PostgreSQL, assurez-vous que le serveur est démarré
# 5. Créez la base de données avec le script SQL fourni

# ===========================================
# SCRIPT DE CRÉATION POSTGRESQL
# ===========================================

# Pour créer la base de données PostgreSQL, exécutez :
# psql -U postgres -c "CREATE DATABASE trainstation;"
# psql -U postgres -d trainstation -f SQL/Creation_script.sql
